import SearchBar from "../searchBar";

const Header = () => {
  return (
    <header className="header">
      <title>Header</title>
      <SearchBar></SearchBar>
    </header>
  );
};

export default Header;
